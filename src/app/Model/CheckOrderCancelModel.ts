export interface CheckOrderCancelModel {
    CustomerCode: string
    ProjectCode: string
    OrderNumber: string[]
    ProdType: string[]
    StructureElement: string[]
    ScheduledProd: string[]
    OrderSource: string[]
}