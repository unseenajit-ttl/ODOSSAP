import { AppDragAndDropDirective } from './app-drag-and-drop.directive';

describe('AppDragAndDropDirective', () => {
  it('should create an instance', () => {
    const directive = new AppDragAndDropDirective();
    expect(directive).toBeTruthy();
  });
});
